
<h1><%= nick_name%></h1>


<% if @user.tweets.any? %>
<% @user.tweets.each do |tweet| %>
    <%= tweet.message  %>
    <br>
    <%= tweet.created_at.strftime("%B %d %Y, %l:%M%P") %>
    <% end %>
    <% end %>
    <% end %>