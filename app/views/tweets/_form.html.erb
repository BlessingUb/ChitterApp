<%= form_with model: @tweet do |form| %>
  <div class="input-group col-xs-12">
    <div class="col-xs-8"><%= form.text_area :message, class: "form-control" %></div>
    <div class="col-xs-4"><%= form.submit "Tweet", class: "btn btn-primary btn-lg" %></div>
  </div>
<% end %>
