<%= form_for Tweet.new do |form| %>
  <div class="input-group col-xs-12">
    <div class="col-xs-8"><%= form.text_field :message, class: "form-control" %></div>
    <div class="col-xs-4"><%= form.submit "Tweet", class: "btn" %></div>
  </div>
<% end %>
